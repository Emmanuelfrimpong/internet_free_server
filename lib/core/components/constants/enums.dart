
enum ToastType { success, error, warning, info }
enum AdminHomePages {dashboard,students,instructors,courses,materials,classes, settings}

enum UserType{admin,student,instructor}

enum MainHomePages {materials,courses,community,classes, auth,profile}